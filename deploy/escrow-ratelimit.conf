# http-context rate-limit zone: only the login endpoint is throttled (brute-force).
# General browsing/enrollment is not limited (internal, session-protected).
limit_req_zone $binary_remote_addr zone=login:10m rate=60r/m;
